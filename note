tuple [相当于数组] list就是list
dict 相当于map set就是set

*args *号接受的是一个tuple
def cal(*args)
如果已经有了list或者tuple
calc(nums[0], nums[1], nums[2])
可以使用calc(*nums)来简化

**kw **可以传入一个key-value形式的，kw将获取一个dict


当你在编写一个程序时，执行语句部分思路还没有完成，这时你可以用pass语句来占位，也可以当做是一个标记，是要过后来完成的代码。比如下面这样：
>>>def iplaypython():
>>>       pass